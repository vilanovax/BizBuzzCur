/**
 * Scoring Layer
 *
 * Converts raw test answers into normalized scores.
 * Pure functions, no side effects.
 */

export { scoreTest, type ScoringResult } from './scorer';
export { normalizeScores } from './normalizer';
